{
  "name": "contracts",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:sepolia": "hardhat run --network sepolia scripts/deploy.ts",
    "deploy:oracle:sepolia": "hardhat run --network sepolia scripts/deploy-oracle.ts",
    "test": "hardhat test",
    "lint": "echo 'No contract lint configured'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "5.0.2",
    "dotenv": "^16.6.1",
    "hardhat": "^2.24.1",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.4.0"
  }
}
